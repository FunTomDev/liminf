[project]
name = "liminf"
version = "0.1.0"
description = ""
authors = [
    {name = "Aleksandr Kuraskin",email = "kuraskin0205@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10, <4.0.0"
dependencies = [
    "django (>=5.1.7,<6.0.0)",
    "django-restframework (>=0.0.1,<0.0.2)",
    "psycopg2 (>=2.9.10,<3.0.0)",
    "django-tests (>=1.8.3,<2.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
]

[tool.poetry]
packages = [{include = "liminf", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
